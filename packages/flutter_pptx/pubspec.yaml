name: flutter_pptx
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: ">=3.0.0-171.0.dev <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dart_pptx:
    path: ../dart_pptx
  screenshot: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
